name: env-example
version: 1
steps:
  - kind: env
    name: check-required
    required: API_KEY, DATABASE_URL
  - kind: echo
    name: env-ok
    text: "All required environment variables are present"